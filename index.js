// 1) has a functional expression called divide
// 2) divide divides 2000 by 100
const divide = function () {
    return 2000 / 100;
};

// 1) has an arrow function called square
// 2) square arrow function multiplies a number times itself
const square = number => number * number;

// 1) has an arrow function called add
// 2) add arrow function takes two parameters and adds them together
const add = (parameter1, parameter2) => parameter1 + parameter2;